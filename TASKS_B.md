# TASKS_B.md — UI · 연출 · 메타 담당 (⚠️ D4~D7 부재판)

> **먼저 `CLAUDE.md`를 읽는다.** 상세 기획은 `GAME_DESIGN.md`.
> **담당: `/src/ui/`, `/src/fx/`, `/assets/` 만.** `/src/game/`은 읽기만.
> ⭐ **A의 코어를 기다리지 않는다.** `?mock=1`로 `MockGameCore`에 붙여 전부 만들 수 있다.

---

## 🚨 이 문서가 기존판과 다른 이유

**B는 D4~D7에 부재한다. 즉 제출일까지 돌아오지 않는다.**

| | 기존 가정 | 실제 |
|---|---|---|
| B의 작업일 | D1~D7 (7일) | **D1~D3 (3일)** + 부재 중 폰 작업 |
| 밸런싱(P4) | 둘이 함께 | **A 단독** |
| 영상·제출(P5) | B 주도 | **A 단독** |
| 인계 | 복귀 후 회수 | **회수 없음. 편도 인계** |

→ **P1·P2·P3를 3일 안에 끝내고, D3 저녁에 완전 인계한다.**
→ D4 이후 새 기능 없음. A는 B가 만든 것을 **조립·촬영**만 한다.

---

## 페이즈 구조 (압축판)

| 페이즈 | 내용 | 완료 조건 | 기준일 |
|---|---|---|---|
| **P0** | 계약 (둘이 함께) | 스키마·인터페이스 확정 | **D1 오전** |
| **P1** | 화면·조작 | Mock으로 배치·HUD가 돈다 | **D1 종료** |
| **P2** | 차별점 연출 ⭐ | 조명 체력바 + 특효 피드백 | **D2 종료** |
| **P3** | 드래프트·보스 + **게임필** | 오버레이·기록·이징·스쿼시 | **D3 오후** |
| **P3.5** | **인계 패키지** | `HANDOFF.md` + 컷리스트 + PDF 초안 + main 머지 | **D3 저녁 (하드 데드라인)** |
| **P4** | 밸런싱 | **A 단독** — B는 폰으로 영상/스샷 리뷰만 | D4~D6 |
| **P5** | 영상·제출 | **A 단독** — B는 폰으로 에셋 추가 납품 | D7 |

---

## P0 — 계약 (D1 오전, 둘이 함께, 3~4시간)

- [ ] 저장소·Vite·Phaser 셋업 (A와 함께)
- [ ] **`/data/*.json` 8종 스키마 확정** — 여기서 합의한 필드만 UI에서 읽는다
- [ ] **`EventBus` 이벤트 목록 확정** — B가 구독할 것들 (`bossLeaked` 포함)
- [ ] **`GameCore` 인터페이스 확정** — B가 호출할 것들
- [ ] 브랜치 `feat/ui` 생성
- [ ] 아트 방향 결정: **추상 맵 — 단색 실루엣 + 단일 액센트 컬러**
- [ ] 🚨 **`map.json` 최종 좌표 확정 + 파생 2건** — 좌표는 나왔다. 문제는 파생이다. **이 3개를 D1에 전부 못 박지 않으면 D2 연출 작업이 헛돈다.**
      - [ ] **좌표 반영** — `CLAUDE.md §4`(92~95줄) · `GAME_DESIGN.md §20`에 **동시** 반영 (현재 두 파일 모두 옛 4점 경로)
      - [ ] **격자 정렬 결정** — 원안은 `y=320`·`x=1080`·`x=200`·`x=1120`·`nseoulTower y=440`이 전부 셀 **경계**에 걸린다 (`cell:40` → 중심은 `40k+20`).
            경로 중심선이 경계 위를 달리면 §5-3 장애물 스냅이 모호해지고 `BuildUI` 미리보기가 반 칸 어긋난다.
            **정렬 보정판 채택 권장:** `[[0,140],[1100,140],[1100,340],[220,340],[220,540],[1140,540]]` / `core {1180,540}` / `nseoulTower {1220,460}`
      - [ ] **`enemies.json` `speed` 재조정 요구** — 경로 길이 685px → **3,300px (4.8배)**. 현행 `trash:25`면 완주 **131초**.
            §6-5상 `speed`는 **A 소유 필드**라 내가 못 고친다. **"웨이브 1~5 합계 3분 이내"를 합격선으로 못 박고 숫자를 A에게 받아낸다.**
- [ ] ⭐ **`UITheme.js` 단일 상수 파일 생성** — 색 팔레트·이징 커브·폰트·간격·애니메이션 시간을 **전부 여기 한 곳에**
      ※ 부재 중 A가 UI를 만져도 감각 없이 일관성이 유지되는 유일한 장치. **P0에 만든다.**
- [ ] ⭐ **AI 에셋 스타일 기준 확정** — 색·톤·해상도·실루엣 규칙 + 프롬프트 템플릿 **14종**
      (랜드마크 6 = N서울타워 + 5 · 적 3 + 보스 1 · 서포터 2 · 장애물 2)
      ※ 에셋 생성은 **폰으로 가능한 유일한 B 작업**이다. 기준만 잡아두면 부재 중에도 납품 가능

---

## P1 — 화면 · 조작 (D1 오후, Mock 기반)

### 씬
- [ ] `TitleScene` — 날짜 표시 `2026.07.28 서울 — 오늘의 방어`, 최고 기록
- [ ] `GameScene` — 맵 배경, S자 경로, 코어, N서울타워 위치
- [ ] `GameOverScene` — 결과 + **2초 내 재시작 버튼**
- [ ] `?mock=1` 로 `MockGameCore` 연결 확인

### 배치 UX
- [ ] `BuildUI.js` — 건물 선택 → **격자 스냅 미리보기**(가능/불가 색 구분)
- [ ] **사거리 원 표시** (배치 중·선택 시)
- [ ] **오라 반경 원 표시** (세운상가) — ⚠️ 없으면 플레이어가 시스템을 이해 못 함
- [ ] 재배치(드래그) · `UpgradeUI.js` 강화 패널(비용·다음 레벨 수치)

### HUD·조작
- [ ] `HUD.js` — 골드 · 웨이브 · 계절 · **XP 바 + 레벨** · 조명 상태
- [ ] `Controls.js` — 배속 1x/2x/3x · 일시정지 · **즉시 다음 웨이브**(보너스 골드 표시)

> 💡 **P1은 A에게 위임 가능한 유일한 구간이다.** D1에 시간이 모자라면 씬 구조·HUD 뼈대·배속/일시정지 버튼·사거리 원을 A에게 넘기고 **B는 P2로 직행한다.** (기계적 작업, 감각 불필요)

---

## P2 — 차별점 연출 ⭐ D2 전부를 여기 쓴다

**이 페이즈가 이 프로젝트에서 B의 존재 이유다. 여기서 밀리면 부재가 치명상이 된다.**

- [ ] **`SeoulTowerLight.js`** — 조명 4단계 파랑→초록→노랑→빨강
      ※ 부드러운 색 보간 + 단계 하강 시 짧은 경고음
      ※ **숫자 체력바를 만들지 않는다**
- [ ] **`DamageNumber.js`** — 풀링. **특효 = 노란 숫자 + "효과가 굉장했다!"** / **크리 = 큰 숫자**
      ※ 이게 없으면 상성 시스템 자체가 플레이어에게 안 보인다
- [ ] `Particles.js` — 피격 흰색 반짝임, 처치 파티클, 골드·XP 팝업
- [ ] `StatusFx.js` — 스턴 표시, DoT 표시, 장애물 쿨다운 게이지
- [ ] `SkyTint.js` — **조명 연동만.** 계절 팔레트 보간은 **2종으로 축소**
- [ ] ~~야간/주간 맵 분기~~ → **삭제.** 주간 고정

---

## P3 — 드래프트 · 보스 · 메타 + 게임필 (D3 오전~오후)

- [ ] **`DraftOverlay.js` — 하나의 컴포넌트로 둘 다 처리**
      ※ 레벨업 = **3장** / 보스 정책 = 3장 → **장수가 같아져서 레이아웃이 하나로 끝난다.** 데이터만 갈아끼운다
      ※ 카드 수는 `waves.json`의 `draftCardCount`/`policyCardCount`에서 읽는다. 하드코딩 금지
      ※ 카드에 효과 + **실제 근거 한 줄**(교육 2층)
      ※ 선택 즉시 전투 복귀 (화면 전환 없이 오버레이)
      ※ **오버레이 = 게임 일시정지.** 레벨업이 여러 번 쌓이면 **큐로 순차 처리**. 순서는 레벨업 → 정책
- [ ] `BossAlert.js` — 보스 등장 경고 배너 + 약한 화면 흔들림 + 하늘 어두워짐
      ※ **`bossLeaked` 수신 시 쿵 + 화면 흔들림 + 보스 소멸 연출**
- [ ] 레벨업 연출 (일시정지 + 오버레이 슬라이드인)
- [ ] 최고 기록 `localStorage` 저장 · **신기록 연출**
- [ ] **AI 에셋 필수 8종 투입** — 적 3 + 보스 1 + 랜드마크 4(청계천·N서울타워·세운상가+1)
      ※ 나머지 **6종**은 부재 중 폰으로 납품
- [ ] ⭐ **에셋 자동 교체 파이프라인 — A의 손을 아예 안 거치게 만든다**
      - [ ] 파일명 규약: `assets/towers/<id>.png` = `towers.json`의 `id`와 **1:1** (적·서포터·장애물도 동일)
      - [ ] `preload`에서 `loaderror` 캐치 → **단색 실루엣 플레이스홀더 텍스처 자동 생성**
      - [ ] 검증: PNG를 지웠다 넣었다 해서 **코드 변경 0**으로 반영되는지 확인
      ※ 이게 되면 부재 중 내가 **GitHub 웹 UI에서 폰으로 PNG만 올려도** 배포에 반영된다. A는 아무것도 안 해도 된다
      ※ 납품 우선순위 = **영상 등장 순서**: N서울타워 → 청계천 → 적 3종 → 보스 → 나머지 랜드마크 → 서포터 → 장애물
- [ ] ⭐ **게임필을 P5에서 P3으로 끌어올린다** (원래 P5 항목, A가 못 만든다)
      - [ ] 타워 배치 "쿵" 스쿼시 애니메이션
      - [ ] 모든 UI 전환에 이징 (`UITheme.js` 커브 사용)
      - [ ] 계절 전환 하늘 색조 페이드

- [ ] ❌ `CodexUI.js` — **삭제.** 3일에 넣을 자리 없음
- [ ] ❌ 역사 변천 색조 변화 — **`towers.json`의 `tint`/`scale`만 적용하는 5줄짜리로 축소.** 자막 없음

> ✅ **P3 완료 = B의 모든 코드 작업 종료. 이후 B는 코드를 쓰지 않는다.**

---

## P3.5 — 인계 패키지 ⭐ D3 저녁, 하드 데드라인

**이걸 안 하고 떠나면 앞의 3일이 절반만 살아남는다.**

### 1. Git — 미완성 브랜치를 남기지 않는다
- [ ] `feat/ui` → `main` **머지 완료.** 컴파일 되고 `?mock=1`로 돌아가는 상태
- [ ] 열린 PR·미커밋 변경 0
- [ ] `SYNC.md §1`에 명시: **"D4~D7 B 부재. `/src/ui/`·`/src/fx/`·`/assets/` 소유권 A로 전면 이관. 복귀 없음."**

### 2. `HANDOFF.md` 작성
- [ ] `/src/ui/`·`/src/fx/` **파일별 1줄 설명**
- [ ] **🟢 A가 고쳐도 되는 것** — 색값, 폰트 크기, 위치 좌표, 애니메이션 시간 (전부 `UITheme.js`)
- [ ] **🔴 A가 건드리면 안 되는 것** — `SeoulTowerLight.js` 보간 로직 / `DamageNumber.js` 풀링 / `DraftOverlay.js` 큐 처리
      ※ 이유까지 적는다. "왜 안 되는지"를 모르면 A는 결국 고친다
- [ ] **알려진 미완성 목록** — 어디까지 됐고 어디부터 빈칸인지
- [ ] **에셋 교체 규칙** — 파일명·해상도·투명 배경 규격표 (B가 폰으로 보낼 것들이 여기 들어감)
- [ ] ⭐ **`UITheme.js` 증상→상수 매핑표** — A가 코드를 안 읽고도 튜닝하게 만드는 장치
      ```
      "조명 단계 전환이 눈에 안 띈다"  → LIGHT.flashAlpha ↑ , LIGHT.transitionMs ↓
      "특효 숫자가 안 보인다"          → DMG.effectiveScale ↑ , DMG.lifeMs ↑
      "화면이 너무 정신없다"           → PARTICLE.count ↓
      "드래프트 카드가 안 읽힌다"      → CARD.fontSize ↑ , CARD.padding ↑
      "보스 관통 임팩트가 약하다"      → SHAKE.bossLeaked ↑
      ```

### 2-1. 부재 중 A의 `/src/ui/` 수정 규칙 (전면 이관의 안전장치)

**소유권은 넘기되 추적 가능성은 남긴다. 이게 없으면 복귀 후 내 코드가 남의 코드가 된다.**

- [ ] **1순위: `UITheme.js` 숫자만 바꾼다.** 해결되면 거기서 멈춘다
- [ ] 로직을 고쳐야 하면 — **새 파일을 만들지 않고** 기존 파일을 고치고, 수정 지점에 `// [A-D5]` 형식 주석을 남긴다 → 복귀 후 **grep 한 번**으로 전부 회수
- [ ] **커밋 prefix 강제: `ui(A): ...`** → `git log --grep="ui(A)"` 로 diff 일괄 추적
- [ ] 🔴 위 3개 파일(조명·데미지숫자·드래프트)은 **상수 조정만.** 구조를 바꿔야 할 것 같으면 `SYNC.md §2`에 적고 **다른 걸 먼저 한다**

### 3. 영상 컷리스트 — A가 "찍기만" 하면 되게
- [ ] **초 단위 콘티 30~60초**
      `0-5s 타이틀 → 5-15s 청계천 특효(노란 숫자) → 15-25s 레벨업 드래프트 → 25-35s 통나무+광역기 콤보 → 35-45s 보스·정책 3장 → 45-55s 조명 빨강 → 55-60s 신기록`
- [ ] 각 컷의 **촬영 조건** 명시 — 몇 웨이브, 어떤 배치, 배속 몇 배, 디버그 플래그
      ※ A가 "어느 장면이 예쁜지" 판단하지 않게 한다. 그게 감각이고, 그건 넘길 수 없다
- [ ] 녹화 해상도·프레임·자막 유무 지정

### 4. 문서 초안
- [ ] **게임 소개 PDF — B 파트(비주얼·연출·차별점) 완성.** A는 자기 파트만 채우면 됨
- [ ] **팀원 롤 기술서 PDF — B 항목 완성**
- [ ] 스크린샷 기준선: **D3 시점 정상 화면 5장 캡처 → `/docs/baseline/`**
      ※ A가 D6에 "화면이 이상한데 원래 이랬나?"를 판단할 유일한 근거

### 5. A에게 남기는 절단 권한
- [ ] **"밀리면 이 순서로 버려라" 목록을 A에게 명시적으로 위임** (아래 참조)
      ※ B가 없을 때 A가 무엇을 버려도 되는지 모르면, **버리면 안 되는 걸 버린다**

---

## D4~D7 — 부재 중 B가 할 수 있는 것 (폰만)

| 가능 | 불가 |
|---|---|
| AI 에셋 나머지 5종 생성·전송 | 코드 작성 |
| A가 보낸 스크린샷·영상 클립 리뷰 | 밸런싱 플레이테스트 |
| PDF 문안 교정 | 연출 튜닝 |
| 영상 컷 최종 승인 | 긴급 버그 대응 |

- [ ] **하루 1회 비동기 체크인** — A가 `SYNC.md`에 진행 상황 1줄, B가 폰으로 확인
- [ ] **D6 저녁: 영상 러프컷 승인** — 이것만은 반드시 본다. 심사 인상의 대부분이 여기서 결정됨

---

## 📏 P4 밸런싱 합격 기준 — "느낌"을 숫자로 바꾼 것

**원래 TASKS_B에는 "A가 못 느끼는 걸 B가 느껴야 한다"고 적혀 있었다. 그 B가 없다.**
그러니 감각 판정을 **A 혼자 O/X로 찍을 수 있는 수치 체크리스트**로 변환해서 넘긴다. 이게 P4를 살리는 유일한 방법이다.

- [ ] 웨이브 1~5 총 소요 **3분 이내** (심사자 첫인상 구간 — `enemies.json` `speed`와 직결)
- [ ] 웨이브 5 이전에 **레벨업 드래프트 최소 2회** 발생
- [ ] 웨이브 5 = 첫 보스. 여기까지 **조명 3 이상** 유지 가능
- [ ] 특효 발동 시 노란 숫자가 **화면 어디를 보고 있어도** 인지됨
- [ ] 조명 4→3 전환을 **모르고 지나칠 수 없음** (스킵되면 `LIGHT.flashAlpha`를 올린다)
- [ ] 관통 0 웨이브 **회복 연출**이 보상으로 인지됨
- [ ] 드래프트 카드 설명이 **1초 안에** 이해됨
- [ ] 오라 반경 원과 사거리 원이 **서로 헷갈리지 않음**
- [ ] 웨이브 20 기준 화면 오브젝트 100+ 에서 **프레임 유지**
- [ ] 모바일 터치로 **격자 스냅이 손가락으로 됨**

> ⛔ **위 항목 중 X가 나오면 `UITheme.js` 상수부터 만진다.** 로직 수정은 최후 수단이다.

---

## 🔥 비상 절단 순서 (A에게 위임하는 권한 포함)

**B 부재 중 A가 이 순서대로 버릴 수 있다. B의 승인 불필요.**

1. ~~도감(CodexUI)~~ — 이미 삭제
2. 사운드 (경보음 1종만 남김)
3. 계절 하늘 팔레트 → 1종 고정
4. 역사 변천 색조 변화
5. ~~야간/주간 분기~~ — 이미 삭제
6. 하늘 틴트 → **조명만 유지**
7. 스쿼시 애니메이션

**🚫 절대 안 버림 (A도 못 버림 — 버리면 이 게임의 차별점이 0이 된다):**
**조명 4단계 · 특효/크리 데미지 숫자 · 드래프트 오버레이 · 사거리·오라 원 · 2초 내 재시작 · 배속**

---

## 💡 v2 아이디어 (떠오르면 코드 대신 여기에)
-
